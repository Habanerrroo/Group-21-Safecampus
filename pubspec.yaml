name: safecampus
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'

# The following defines the version and build number for the application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that the package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  # UI & Design
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9

  # State Management
  provider: ^6.1.1

  # Backend & Database
  supabase_flutter: ^2.3.4
  shared_preferences: ^2.2.2

  # Utilities
  intl: ^0.19.0

  # Media & Camera
  image_picker: ^1.0.7
  permission_handler: ^11.3.0

  # Location
  geolocator: ^11.0.0
  geocoding: ^3.0.0

  # Audio/Video Recording
  record: ^5.0.0
  path_provider: ^2.1.2
  audioplayers: ^5.2.1

  # Chat & Real-time
  flutter_chat_ui: ^1.6.11
  uuid: ^4.3.3

  # URL Launcher
  url_launcher: ^6.2.4

  # Charts & Analytics
  fl_chart: ^0.66.0

  # Animations
  lottie: ^3.0.0
  smooth_page_indicator: ^1.1.0

dependency_overrides:
  # Fix for record_linux compatibility issue
  record_linux: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file.
  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with the application, so that the icons can be used in
  # the material Icons class.
  uses-material-design: true
